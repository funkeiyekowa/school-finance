import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";const root=path.resolve(__dirname,"..","..","..");const sql=fs.readFileSync(path.join(root,"supabase","20260922024500_phase2_teaching_plans.sql"),"utf8"),route=fs.readFileSync(path.join(root,"src","app","api","lms","teaching-plans","route.ts"),"utf8"),page=fs.readFileSync(path.join(root,"src","app","dashboard","teaching","plans","page.tsx"),"utf8");assert.match(sql,/CREATE TABLE IF NOT EXISTS public\.lms_teaching_plans/);assert.match(sql,/phase2_save_teaching_plan/);assert.match(sql,/phase2_transition_teaching_plan/);assert.match(sql,/FOR UPDATE/);assert.match(sql,/public\.is_org_admin/);assert.match(sql,/phase1_teacher_course_scope/);assert.match(sql,/changes_requested/);assert.match(route,/requireStaffSessionWithOrg/);assert.doesNotMatch(route,/SUPABASE_SERVICE_ROLE_KEY|p_organization_id/);assert.match(page,/Teaching Plan Studio/);assert.match(page,/Approve/);assert.match(page,/Return/);console.log("Teaching-plan moderation and authorization contracts passed.");
