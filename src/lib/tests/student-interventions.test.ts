import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";const root=path.resolve(__dirname,"..","..","..");const sql=fs.readFileSync(path.join(root,"supabase","20260922030000_phase2_lms_interventions.sql"),"utf8"),route=fs.readFileSync(path.join(root,"src","app","api","lms","interventions","route.ts"),"utf8"),page=fs.readFileSync(path.join(root,"src","app","dashboard","teaching","interventions","page.tsx"),"utf8");assert.match(sql,/lms_student_interventions/);assert.match(sql,/phase2_save_student_intervention/);assert.match(sql,/phase1_teacher_course_scope/);assert.match(sql,/my_linked_student_ids/);assert.match(sql,/family_visible/);assert.match(sql,/FOR UPDATE/);assert.match(sql,/Resolution summary is required/);assert.match(sql,/Owner must be an active organization member/);assert.match(sql,/Intervention student cannot be changed/);assert.match(route,/requireStaffSessionWithOrg/);assert.match(route,/create_direct_conversation/);assert.match(route,/send_message/);assert.match(route,/best-effort/);assert.doesNotMatch(route,/SUPABASE_SERVICE_ROLE_KEY|p_organization_id/);assert.match(page,/Student Interventions/);assert.match(page,/Visible to linked family and student/);console.log("Persistent student-intervention authorization contracts passed.");
