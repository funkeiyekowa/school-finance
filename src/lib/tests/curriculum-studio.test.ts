import assert from"node:assert/strict";import fs from"node:fs";import path from"node:path";const root=path.resolve(__dirname,"..","..","..");const sql=fs.readFileSync(path.join(root,"supabase","20260922023000_phase2_lms_curriculum.sql"),"utf8"),route=fs.readFileSync(path.join(root,"src","app","api","lms","curriculum","route.ts"),"utf8"),page=fs.readFileSync(path.join(root,"src","app","dashboard","teaching","curriculum","page.tsx"),"utf8");for(const t of["lms_curriculum_units","lms_learning_objectives"])assert.match(sql,new RegExp(`CREATE TABLE IF NOT EXISTS public\\.${t}`));assert.match(sql,/phase2_save_curriculum_unit/);assert.match(sql,/phase1_teacher_course_scope/);assert.match(sql,/current_user_org_id/);assert.match(sql,/REVOKE ALL ON FUNCTION/);assert.match(route,/requireStaffSessionWithOrg/);assert.doesNotMatch(route,/SUPABASE_SERVICE_ROLE_KEY|p_organization_id/);assert.match(page,/Curriculum Studio/);assert.match(page,/Learning objective/);console.log("Curriculum Studio authorization and objective contracts passed.");
